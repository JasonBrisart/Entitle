"""
Entitle GUI package.

A minimal Tkinter front end for the core Entitle workflows. This package was made
an explicit package in 0.4.0 (previously it relied on implicit namespace-package
behavior) so its import semantics match the ``entitle`` package and the intended
tab plug-in registry in ``gui.app``.
"""
